[package]
name = "mst5-client-interop-tests"
version = "0.0.0"
edition = "2021"
publish = false

[dev-dependencies]
getrandom = "0.3.3"
micromsg = { path = "../../micro-chat/micromsg" }
mst5-client = { path = ".." }
tokio = { version = "1.47", features = ["macros", "net", "rt-multi-thread", "time"] }
x25519-dalek = { version = "2.0.1", default-features = false, features = ["static_secrets"] }
